package ai.droidforge.tools.android

/** Generic key-press targets. A real Android-backed controller maps these to `KeyEvent` constants; this module names none of those constants directly. */
enum class DeviceKey { BACK, HOME, ENTER, DELETE, TAB, VOLUME_UP, VOLUME_DOWN, POWER, RECENT_APPS }
